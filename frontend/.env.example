# Copy to frontend/.env for local dev (already the tracked default), or to frontend/.env.production
# before `npm run build` when deploying, pointed at your real API origin.
VITE_API_BASE_URL=http://127.0.0.1:8001
